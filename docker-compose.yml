services:
  hysteria:
    image: ghcr.io/darknightlab/hysteria-docker:main
    container_name: hysteria2
    restart: always
    network_mode: host
    environment:
      - apiHost=
      - apiKey=
      - nodeID=
      - domain=
      - email=
      - cloudflare_api_token=